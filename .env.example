# ============================================================
# Game Server Manager - Configuration
# ============================================================
# Copy this file to .env and fill in your values
# Then run: docker compose up -d
# ============================================================

# MongoDB credentials
MONGO_INITDB_ROOT_PASSWORD=changeme_generate_random_password

# Application secrets (generate random values)
SESSION_SECRET=changeme_generate_random_secret
JWT_SECRET=changeme_generate_random_secret

# Backup storage path
BACKUP_PATH=/mnt/backup/container

# SteamGridDB API key (for game icons) -> https://www.steamgriddb.com/profile/preferences/api
STEAMGRIDDB_API_KEY=changeme_get_your_key_from_steamgriddb

# ============================================================
# QUICK SETUP:
# 1. Copy: cp .env.example .env
# 2. Edit .env and replace all "changeme_*" values with random strings:
#      
#      Generate with these commands:
#      MONGO_INITDB_ROOT_PASSWORD: openssl rand -hex 24
#      SESSION_SECRET:             openssl rand -hex 48
#      JWT_SECRET:                 openssl rand -hex 48
#
# 3. Start: docker compose up -d
# 4. Configure Nginx Proxy Manager at http://YOUR_IP:81
#    - On first visit, register your admin account
#    - Add proxy host for your domain → gsm-frontend:80
#    - Enable SSL with Let's Encrypt
# 5. Access: https://your-domain.com
# ============================================================

# Optional: Disable IPv6 in Nginx Proxy Manager
# DISABLE_IPV6=true