name: Build and Publish Docker Images

on:
  push:
    tags:
      - 'v*.*.*'
  release:
    types: [created, published]
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/test.yml

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    env:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
      GHCR_USERNAME: ${{ github.actor }}
      GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    strategy:
      matrix:
        service:
          - name: backend
            context: ./server
            dockerfile: ./server/Dockerfile
            image: gsm-backend
          - name: frontend
            context: ./client
            dockerfile: ./client/Dockerfile
            image: gsm-frontend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ env.DOCKER_TOKEN }}
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ env.GHCR_USERNAME }}
          password: ${{ env.GHCR_TOKEN }}
      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.DOCKER_USERNAME }}/${{ matrix.service.image }}
            ghcr.io/${{ github.repository_owner }}/gsm-${{ matrix.service.name }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable={{is_default_branch}}
      - name: Build and push ${{ matrix.service.name }} image
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.service.context }}
          file: ${{ matrix.service.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Scan container image for vulnerabilities
        uses: Azure/container-scan@v0
        with:
          image-name: ${{ env.DOCKER_USERNAME }}/${{ matrix.service.image }}:latest
          run-quality-checks: true
          severity-threshold: CRITICAL
          fail-on-severity: true
      - name: Attest build provenance for ${{ matrix.service.name }}
        uses: actions/attest-build-provenance@v1
        with:
          subject-name: ${{ steps.meta.outputs.tags }}
          subject-digest: ${{ steps.meta.outputs.digest }}
          push-to-registry: true
      - name: Image digest
        run: echo "Image pushed with digest ${{ steps.meta.outputs.digest }}"
      - name: Update Docker Hub Description
        uses: peter-evans/dockerhub-description@v5
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ env.DOCKER_TOKEN }}
          repository: ${{ env.DOCKER_USERNAME }}/${{ matrix.service.image }}
          readme-filepath: ./README.md
